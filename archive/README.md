# Archive

This folder contains material from the original face-recognition prototype that was moved out of the active product surface during the Averra restructure.

- `legacy/` contains the original experimental Python files.
- `data/` is reserved for local exports and is intentionally not part of the active database.

Nothing in this folder is imported by the production application.

